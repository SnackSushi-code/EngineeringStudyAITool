# Voice Architecture Baseline

Voice is provider-independent.

Interfaces:
- STT
- TTS
- prerecorded audio
- VAD
- interruption/barge-in
- wake mode
- audio device routing

The supplied MP3 voice asset is treated as a configurable user-provided voice resource. The core must not be coupled to one vendor.
